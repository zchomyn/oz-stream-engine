// APE STREAM ENGINE — product_plan.js stub. World-reshape via product plan
// is a campaign concept; not applicable. All exports are no-ops.

const PRODUCT_PLAN_SCHEMA = { type: "object", properties: {}, required: [] };
const HOME_ROOMS = [];
const WEEKDAYS = [];

function productPlanPrompt() { return ""; }

module.exports = { PRODUCT_PLAN_SCHEMA, productPlanPrompt, HOME_ROOMS, WEEKDAYS };
